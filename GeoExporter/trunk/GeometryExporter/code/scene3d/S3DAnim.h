#pragma once

#include <Vec3.h>
#include <Quat.h>

class S3DAnim
{
public:
/*	void GetPosition(float time, Vec3 &position);
	void GetRotation(float time, float &angle, Vec3 &axis);
	void GetScale(float time, Vec3 &scale);
	void GetScalar(float time, float &scalar);

	bool HavePosition();
	bool HaveRotation();
	bool HaveScale();
	bool HaveScalar();

	void SetPositionInterpolator(InterVec3 *position_interpolator);
	void SetRotationInterpolator(InterQuat *rotation_interpolator);
	void SetScaleInterpolator(InterVec3 *scale_interpolator);
	void SetScalarInterpolator(InterFloat *scalar_interpolator);

private:
	InterFloat *position_interpolator;
	InterVec3 *rotation_interpolator;
	InterVec3 *scale_interpolator;
	InterQuat *scalar_interpolator;*/
};
